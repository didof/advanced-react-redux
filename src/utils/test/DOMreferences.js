const commentBox = {
	button: {
		fetchComments: 'fetch_comments-button',
	},
};

const commentList = {
	wrapper: 'wrapper_comment-list',
	list: 'list_comment-list',
	listItem: 'list-item_comment-list'
};

const DOMref = {
	commentBox,
	commentList,
};

export default DOMref;
